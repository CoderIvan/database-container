apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
  namespace: mongodb-system
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  volumeName: mongodb
  resources:
    requests:
      storage: 10Gi
